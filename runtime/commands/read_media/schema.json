{
  "name": "read_media",
  "description": "Read local media. Single files are parsed deeply; directories or multiple files return a compact contact-sheet thumbnail overview.",
  "input_schema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cli": {
        "type": "string",
        "x-tura-optional": true,
        "description": "Optional CLI-form command line. Example: media/downloads --max-files 10 --max-side 512"
      },
      "paths": {
        "type": "array",
        "minItems": 1,
        "maxItems": 8,
        "items": { "type": "string" },
        "description": "Workspace-relative or absolute media paths to inspect."
      },
      "files": {
        "type": "array",
        "minItems": 1,
        "maxItems": 8,
        "items": { "type": "string" },
        "x-tura-optional": true,
        "description": "Alias for paths."
      },
      "include_text": {
        "type": "boolean",
        "x-tura-optional": true,
        "description": "For PDFs and text-like files, include extracted text. Defaults to true."
      },
      "max_text_chars": {
        "type": "integer",
        "minimum": 1000,
        "maximum": 80000,
        "x-tura-optional": true,
        "description": "Maximum extracted text characters per file. Defaults to 40000."
      },
      "max_visuals": {
        "type": "integer",
        "minimum": 0,
        "maximum": 60,
        "x-tura-optional": true,
        "description": "Maximum visual previews for single-file detailed mode. Defaults to 6."
      },
      "max_side": {
        "type": "integer",
        "minimum": 128,
        "maximum": 1024,
        "x-tura-optional": true,
        "description": "Maximum preview image side in pixels. Defaults to 512."
      },
      "max_files": {
        "type": "integer",
        "minimum": 1,
        "maximum": 100,
        "x-tura-optional": true,
        "description": "When a path is a directory, read the newest N files by modification time. Defaults to 20."
      }
    }
  }
}
