id = "apply_patch"
name = "Apply Patch"
description = "Apply a patch to files."
core = true
category = "edit"
execution = "in_process"
state_machine = "default_command"
supports_macro_command = false
mutating = true
network = false

[runtime]
binary = ""
entry = "crate::commands::apply_patch"
language = "rust"

[limits]
default_timeout_ms = 60000
max_timeout_ms = 300000

[paths]
prompt = "prompt.md"
schema = "schema.json"
policy = "policy.toml"

[builder_config]
raw = {}
