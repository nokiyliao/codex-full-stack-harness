package_id = "playwright_node"
description = "Node and Playwright browser automation support for frontend screenshot and interaction workflows."
node = "bun>=1.3"
entry = "bun --cwd scripts/packages/playwright_node run playwright"
allowed_network_during_setup = true
cache = "scripts/packages/playwright_node/node_modules"

dependencies = [
  "playwright",
]
